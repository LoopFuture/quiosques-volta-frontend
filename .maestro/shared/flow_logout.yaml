- runFlow:
    file: ../shared/ensure_ready_user.yaml
- runFlow:
    file: ../shared/go_tab_profile.yaml

- tapOn:
    id: profile-logout-button

- extendedWaitUntil:
    visible:
      id: auth-screen
    timeout: 60000
