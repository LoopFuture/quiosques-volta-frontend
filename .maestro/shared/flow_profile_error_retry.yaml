appId: com.voltafrontend.app
---
- runFlow:
    file: ../shared/ensure_ready_user.yaml

- openLink: 'voltafrontend:///profile?__e2eQueryState=error'

- extendedWaitUntil:
    visible:
      id: profile-screen-error-state
    timeout: 30000
- tapOn:
    id: profile-screen-error-state-retry-button
- extendedWaitUntil:
    visible:
      id: profile-screen
    timeout: 30000
